{
  "name": "midway-subpackage-global-config",
  "version": "1.0.0",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "dependencies": {
  },
  "devDependencies": {
    "midway-bin": "2"
  },
  "scripts": {
    "dev": "NODE_ENV=local midway-bin dev --ts",
    "build": "midway-bin build -c",
    "test": "NODE_ENV=test midway-bin test --ts --full-trace",
    "debug": "NODE_ENV=test midway-bin test --ts --full-trace --inspect-brk=9229",
    "cov": "NODE_ENV=unittest midway-bin cov --ts",
    "clean": "midway-bin clean",
    "autod": "midway-bin autod"
  },
  "engines": {
    "node": ">= 10.9.0"
  },
  "license": "MIT"
}
